{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "duckWalk: build extension",
      "type": "shell",
      "command": "pnpm",
      "args": [
        "--filter",
        "duckwalk-vscode-extension",
        "build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
